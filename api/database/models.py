from .db import mongo

SEX = ('M', 'F')
class Athlete():
    a=1
    # athlete_id =mongo.IntField(required=True, unique=True)
    # access_token = mongo.StringField(required=True)
    # refresh_token = mongo.StringField(required=True)
    # expires_at =mongo.DateTimeField(required=True)
    # first_name = mongo.StringField(required=True)
    # last_name = mongo.StringField(required=True)
    # sex = mongo.StringField(required=True, choices=SEX)
    